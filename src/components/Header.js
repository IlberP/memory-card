import React from 'react'

function Header() {
    return (
        <div>
            <div>Memory Card Game</div>
            <h3>Click on cards to score, but don't click the same card or else you lose!</h3>
        </div>
    )
}

export default Header
